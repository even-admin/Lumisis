---
import { Image } from 'astro:assets';
import { NAV_ITEMS, prefixBase } from '@/lib/constants';
import { getLocaleFromUrl, getTranslatedPath } from '@/i18n/utils';
import { ui } from '@/i18n/ui';
import { lumisisLogo } from '@/lib/images';

const locale = getLocaleFromUrl(Astro.url);
const navItems = NAV_ITEMS[locale];
const alternatePath = getTranslatedPath(Astro.url.pathname);
const switchLabel = ui[locale]['lang.switch'];
const switchAriaLabel = ui[locale]['lang.label'];
const homeHref = locale === 'es' ? prefixBase('/') : prefixBase('/en/');
---

<header class="header" id="site-header" transition:persist>
  <nav class="header__nav" aria-label="Main navigation">
    <a href={homeHref} class="header__logo">
      <Image src={lumisisLogo} alt="LUMISIS" height={28} class="header__logo-img" />
    </a>

    <div class="header__links">
      {navItems.map((item) => (
        <a href={item.href} class="header__link">
          {item.label}
        </a>
      ))}

      <a
        href={alternatePath}
        class="header__lang"
        aria-label={switchAriaLabel}
      >
        {switchLabel}
      </a>
    </div>

    <button
      class="header__burger"
      aria-label="Open menu"
      id="mobile-menu-toggle"
      type="button"
    >
      <span></span>
      <span></span>
    </button>
  </nav>

  <div id="mobile-menu" class="mobile-menu" aria-hidden="true">
    <div class="mobile-menu__top">
      <a href={homeHref} class="header__logo">
        <Image src={lumisisLogo} alt="LUMISIS" height={28} class="header__logo-img" style="filter: none;" />
      </a>
      <button
        aria-label="Close menu"
        id="mobile-menu-close"
        type="button"
        class="mobile-menu__close"
      >
        &times;
      </button>
    </div>
    <nav class="mobile-menu__nav" aria-label="Mobile navigation">
      {navItems.map((item) => (
        <a href={item.href} class="mobile-menu__link">
          {item.label}
        </a>
      ))}
      <a
        href={alternatePath}
        class="mobile-menu__lang"
        aria-label={switchAriaLabel}
      >
        {switchLabel}
      </a>
    </nav>
  </div>
</header>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    background-color: var(--color-surface-dark, #0A0A0A);
    border-bottom: 1px solid var(--color-border-dark, rgba(255, 255, 255, 0.1));
  }

  .header__nav {
    max-width: var(--max-width-site, 80rem);
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.25rem 1.5rem;
  }

  @media (min-width: 768px) {
    .header__nav {
      padding: 1.25rem 2rem;
    }
  }

  .header__logo {
    text-decoration: none;
    display: flex;
    align-items: center;
  }

  .header__logo-img {
    height: 28px;
    width: auto;
    filter: brightness(0) invert(1);
  }

  .header__links {
    display: none;
    align-items: center;
    gap: 2rem;
  }

  @media (min-width: 768px) {
    .header__links {
      display: flex;
    }
  }

  .header__link {
    position: relative;
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    text-decoration: none;
    color: rgba(255, 255, 255, 0.75);
    transition: color 200ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .header__link::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -4px;
    width: 100%;
    height: 1px;
    background-color: currentColor;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 300ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .header__link:hover {
    color: white;
  }

  .header__link:hover::after {
    transform: scaleX(1);
  }

  .header__lang {
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-decoration: none;
    color: rgba(255, 255, 255, 0.4);
    transition: color 200ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .header__lang:hover {
    color: white;
  }

  /* Burger */
  .header__burger {
    display: flex;
    flex-direction: column;
    gap: 6px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 4px;
  }

  @media (min-width: 768px) {
    .header__burger {
      display: none;
    }
  }

  .header__burger span {
    display: block;
    width: 24px;
    height: 2px;
    background-color: white;
  }

  /* Mobile menu */
  .mobile-menu {
    position: fixed;
    inset: 0;
    z-index: 50;
    background-color: white;
    display: none;
  }

  .mobile-menu--open {
    display: block;
  }

  .mobile-menu__top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.25rem 1.5rem;
  }

  .mobile-menu__close {
    font-size: 1.75rem;
    line-height: 1;
    background: none;
    border: none;
    cursor: pointer;
    color: var(--color-brand-black);
    padding: 4px;
  }

  .mobile-menu__nav {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding: 3rem 1.5rem 0;
  }

  .mobile-menu__link {
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 700;
    text-decoration: none;
    color: var(--color-brand-black);
    letter-spacing: -0.02em;
  }

  .mobile-menu__lang {
    margin-top: 1rem;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-decoration: none;
    color: var(--color-gray-400);
  }
</style>

<script>
  function initHeader() {
    const toggle = document.getElementById('mobile-menu-toggle');
    const close = document.getElementById('mobile-menu-close');
    const menu = document.getElementById('mobile-menu');

    toggle?.addEventListener('click', () => {
      menu?.classList.add('mobile-menu--open');
      menu?.setAttribute('aria-hidden', 'false');
    });

    close?.addEventListener('click', () => {
      menu?.classList.remove('mobile-menu--open');
      menu?.setAttribute('aria-hidden', 'true');
    });

    menu?.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        menu.classList.remove('mobile-menu--open');
        menu.setAttribute('aria-hidden', 'true');
      });
    });
  }

  initHeader();
  document.addEventListener('astro:page-load', initHeader);

  document.addEventListener('astro:after-swap', () => {
    const menu = document.getElementById('mobile-menu');
    menu?.classList.remove('mobile-menu--open');
    menu?.setAttribute('aria-hidden', 'true');
  });
</script>
